---
- name: Ensure nvim normal package is not installed
  pacman:
    name: neovim
    state: absent
  become: true
  become_user: root

- name: Installs nvim nightly build
  aur:
    use: yay
    name:
      - neovim-git

- name: Create config directoy if does not exists
  ansible.builtin.file:
    path: /home/david/.config/nvim
    state: directory
    mode: '0755'

- name: Create init.vim file
  ansible.builtin.copy:
    src: ../files/config/nvim/init.vim
    dest: /home/david/.config/nvim/init.vim
    mode: '0644'

- name: Create general settings file
  ansible.builtin.copy:
    src: ../files/config/nvim/general/
    dest: /home/david/.config/nvim/general/
    mode: '0755'

- name: Create plugins settings file
  ansible.builtin.copy:
    src: ../files/config/nvim/plugins/
    dest: /home/david/.config/nvim/plugins/
    mode: '0755'

- name: Create lua configurations
  ansible.builtin.copy:
    src: ../files/config/nvim/lua/
    dest: /home/david/.config/nvim/lua/
    mode: '0755'

- name: Make sure nvim log folder exists
  ansible.builtin.file:
    path: /home/david/.local/share/nvim/log
    state: directory
    mode: '0755'

- name: Install vim-plug
  get_url:
    url: https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim 
    dest: /home/david/.local/share/nvim/site/autoload/plug.vim 
    mode: '0640'
